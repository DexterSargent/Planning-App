services:
  api:
    build: .
    volumes:
      - ./data:/data
      - ./database.py:/app/database.py:ro
    ports:
      - '8000:8000'
    environment:
      - DATABASE_URL=/data/performance_hq.db

  frontend:
    build:
      context: ./frontend
    ports:
      - '3000:3000'
    depends_on:
      - api
    environment:
      - REACT_APP_API_URL=http://api:8000
